/*
int test(int a)
{
	return 0;
}
*/

int foo(int a, int b, int c)
{
	int d;
	int e;
	int f;
	return a + b + c;
}

void main(void)
{
	int a;
	int b;
	int c;

	a = 3;

	while ( a >= 0)
	{
		output(a);
		a = a - 1;
	}

/*
	output(a);
	output(b);
	/*
	a = 10;
	c = 100;
	b = a = 9000;
	a = c;
	output(1010101);
	output(a);
	output(b);
	output(c);

	b = b + 1;
	output(b);
	b = b - 1;
	output(b);
	b = b / c;
	output(b);
	b = b * b;
	output(b);

	a = 100;
	b = 10;
	c = 1;

	a = foo(a, b, c);
	b = c;
	output(a);

	a = 20;
	b = 3;
	c = a + (b * 2);
	output(c);

	c = input();
	output(c);
	output(input());

	output(input() + 1);

	*/
}
